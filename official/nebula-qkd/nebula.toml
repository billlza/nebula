schema_version = 1

[package]
name = "nebula-qkd"
version = "0.1.0"
entry = "src/lib.nb"
src_dir = "src"

[dependencies]
tls = { path = "../nebula-tls" }

[native]
cxx_sources = ["native/bridge.cpp"]
