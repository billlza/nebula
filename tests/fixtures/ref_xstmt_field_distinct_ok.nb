struct Pair {
  a: Int
  b: Int
}

fn bump(ref v: Int) -> Int {
  v = v + 1
  return v
}

fn ref_xstmt_field_distinct_ok() -> Int {
  let p = Pair(1, 2)
  let f = bump
  let y = f(p.a)
  return p.b + y
}
