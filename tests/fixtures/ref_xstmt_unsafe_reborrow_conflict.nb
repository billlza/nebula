@unsafe
fn raw_touch(ref x: Int) -> Int {
  x = x + 1
  return x
}

fn bump(ref x: Int) -> Int {
  x = x + 1
  return x
}

@unsafe
fn ref_xstmt_unsafe_reborrow_conflict() -> Int {
  let x = 1
  let y = raw_touch(x)
  return bump(x) + y
}
