fn noescape(ref x: Int) -> Int {
  x = x + 1
  return 0
}

fn mayescape(ref x: Int) -> Int {
  return x
}

fn ref_summary_loop_join_unknown() -> Int {
  let x = 1
  let f = noescape
  for i in 0..1 {
    f = mayescape
  }
  let y = f(x)
  return x + y
}
