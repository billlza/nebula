struct Box {
  v: Int
}

fn forward_box(ref b: Box) -> Box {
  return b
}

fn ref_summary_direct_mayescape_write() -> Int {
  let b = Box(1)
  let kept = forward_box(b)
  b.v = kept.v + 1
  return b.v
}
