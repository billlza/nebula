struct Box {
  v: Int
}

fn forward_box(ref b: Box) -> Box {
  return b
}

fn touch_box(ref b: Box) -> Int {
  b.v = b.v + 1
  return b.v
}

fn ref_summary_direct_then_indirect_reborrow() -> Int {
  let b = Box(1)
  let kept = forward_box(b)
  let f = touch_box
  return f(b) + kept.v
}
