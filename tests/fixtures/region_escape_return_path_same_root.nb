struct Foo {
  a: Int
}

fn merge_pair(a: Foo, b: Foo) -> Foo {
  return Foo(a.a + b.a)
}

fn return_path_same_root_escape() -> Foo {
  region R {
    let x = Foo(1)
    return merge_pair(x, x)
  }
}
