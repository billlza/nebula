id = "TST-137-install-sh-verify-attestations-fails-fast"
suite = "test"

[[steps]]
kind = "shell"
run = "python3 - <<'PY'\nimport hashlib\nimport os\nimport platform\nimport subprocess\nfrom pathlib import Path\n\nrepo_root = Path(os.environ['NEBULA_REPO_ROOT'])\nsystem = platform.system()\nif system.startswith('MSYS') or system.startswith('MINGW') or system == 'Windows':\n    Path('work/install-attest-fail-skip.txt').write_text('skip\\n', encoding='utf-8')\n    raise SystemExit(0)\nroot = Path('work/install_attest_fail')\nroot.mkdir(parents=True, exist_ok=True)\nmachine = platform.machine().lower()\nif system == 'Darwin' and machine in ('arm64', 'aarch64'):\n    target = 'darwin-arm64'\nelif system == 'Darwin' and machine == 'x86_64':\n    target = 'darwin-x86_64'\nelif system == 'Linux' and machine == 'x86_64':\n    target = 'linux-x86_64'\nelse:\n    raise SystemExit(f'unsupported host for install attestation test: {system}/{machine}')\nsubprocess.run([\n    'python3', str(repo_root / 'scripts' / 'package_release.py'),\n    '--binary', os.environ['NEBULA_BINARY'],\n    '--target', target,\n    '--output-dir', str(root),\n], check=True)\nasset = next(root.glob('nebula-v*.tar.gz'))\nh = hashlib.sha256()\nwith asset.open('rb') as fh:\n    for chunk in iter(lambda: fh.read(1024 * 1024), b''):\n        h.update(chunk)\n(root / 'SHA256SUMS.txt').write_text(f'{h.hexdigest()}  {asset.name}\\n', encoding='utf-8')\nstem = asset.name[:-7]\n(root / 'SHA256SUMS.txt.intoto.jsonl').write_text('{}\\n', encoding='utf-8')\n(root / f'{stem}.provenance.intoto.jsonl').write_text('{}\\n', encoding='utf-8')\n(root / f'{stem}.sbom.intoto.jsonl').write_text('{}\\n', encoding='utf-8')\n(root / 'VERSION.txt').write_text((repo_root / 'VERSION').read_text(encoding='utf-8').strip() + '\\n', encoding='utf-8')\nPY"
expect_rc = 0

[[steps]]
kind = "shell"
run = "if [ -f work/install-attest-fail-skip.txt ]; then exit 0; fi\nmkdir -p work/fake-gh-fail\ncat > work/fake-gh-fail/gh <<'EOF'\n#!/usr/bin/env bash\nprintf 'gh %s\\n' \"$*\" >> \"$NEBULA_TEST_GH_LOG\"\nexit 1\nEOF\nchmod +x work/fake-gh-fail/gh"
expect_rc = 0

[[steps]]
kind = "shell"
run = "if [ -f work/install-attest-fail-skip.txt ]; then exit 0; fi\nversion=$(cat work/install_attest_fail/VERSION.txt)\nNEBULA_INSTALL_VERIFY_ATTESTATIONS=1 NEBULA_TEST_GH_LOG=\"$PWD/work/install-attest-fail-gh.log\" PATH=\"$PWD/work/fake-gh-fail:$PATH\" bash \"$NEBULA_REPO_ROOT/scripts/install.sh\" --version \"$version\" --prefix \"$PWD/work/install-attest-fail-prefix\" --base-url \"$PWD/work/install_attest_fail\" > work/install-attest-fail.out 2>&1"
expect_rc = 1

[[steps]]
kind = "shell"
run = "if [ -f work/install-attest-fail-skip.txt ]; then exit 0; fi\ngrep -F 'attestation verify' work/install-attest-fail-gh.log && test ! -e work/install-attest-fail-prefix/bin/nebula"
expect_rc = 0
