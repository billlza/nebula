id = "TST-228-official-tls-http2-package-smoke"
suite = "test"

[[steps]]
kind = "shell"
run = "python3 - <<'PY'\nimport json\nimport os\nimport platform\nimport shutil\nimport socket\nimport subprocess\nimport time\nfrom pathlib import Path\n\nsystem = platform.system().lower()\nif system.startswith('windows') or system.startswith('msys') or system.startswith('mingw') or system.startswith('cygwin'):\n    print('tls-http2-package-skip-unsupported-host')\n    raise SystemExit(0)\nif shutil.which('openssl') is None:\n    raise SystemExit('openssl is required for official TLS HTTP/2 package smoke test')\n\nrepo_root = Path(os.environ['NEBULA_REPO_ROOT'])\nroot = Path('work/official_tls_http2_package')\nserver_app = root / 'server_app'\nclient_app = root / 'client_app'\nready_path = root / 'server.ready'\nshutil.rmtree(root, ignore_errors=True)\n(server_app / 'src').mkdir(parents=True, exist_ok=True)\n(client_app / 'src').mkdir(parents=True, exist_ok=True)\n\nwith socket.socket(socket.AF_INET, socket.SOCK_STREAM) as probe:\n    probe.bind(('127.0.0.1', 0))\n    port = probe.getsockname()[1]\n\n(root / 'openssl_ca.cnf').write_text('''[req]\\ndistinguished_name = dn\\nx509_extensions = v3_ca\\nprompt = no\\n\\n[dn]\\nCN = Nebula TLS HTTP2 CA\\n\\n[v3_ca]\\nsubjectKeyIdentifier = hash\\nauthorityKeyIdentifier = keyid:always,issuer\\nbasicConstraints = critical, CA:true\\nkeyUsage = critical, digitalSignature, keyCertSign, cRLSign\\n''', encoding='utf-8')\n(root / 'openssl_server.cnf').write_text('''[req]\\ndistinguished_name = dn\\nreq_extensions = v3_req\\nprompt = no\\n\\n[dn]\\nCN = localhost\\n\\n[v3_req]\\nsubjectAltName = @alt_names\\nbasicConstraints = CA:false\\nkeyUsage = critical, digitalSignature, keyEncipherment\\nextendedKeyUsage = serverAuth\\n\\n[alt_names]\\nDNS.1 = localhost\\nIP.1 = 127.0.0.1\\n''', encoding='utf-8')\nsubprocess.run(['openssl', 'req', '-x509', '-newkey', 'rsa:2048', '-nodes', '-keyout', str(root / 'ca.key'), '-out', str(root / 'ca.pem'), '-days', '2', '-config', str(root / 'openssl_ca.cnf')], check=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)\nsubprocess.run(['openssl', 'req', '-newkey', 'rsa:2048', '-nodes', '-keyout', str(root / 'server.key'), '-out', str(root / 'server.csr'), '-config', str(root / 'openssl_server.cnf')], check=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)\nsubprocess.run(['openssl', 'x509', '-req', '-in', str(root / 'server.csr'), '-CA', str(root / 'ca.pem'), '-CAkey', str(root / 'ca.key'), '-CAcreateserial', '-out', str(root / 'server.pem'), '-days', '2', '-sha256', '-extfile', str(root / 'openssl_server.cnf'), '-extensions', 'v3_req'], check=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)\n\nserver_cert = (root / 'server.pem').read_text(encoding='utf-8')\nserver_key = (root / 'server.key').read_text(encoding='utf-8')\nca_pem = (root / 'ca.pem').read_text(encoding='utf-8')\n\ndef lit(text: str) -> str:\n    return text.replace('\\\\', '\\\\\\\\').replace('\\n', '\\\\n').replace('\"', '\\\\\"')\n\n(server_app / 'nebula.toml').write_text(f'''schema_version = 1\\n\\n[package]\\nname = \"official-tls-http2-server-smoke\"\\nversion = \"0.1.0\"\\nentry = \"src/main.nb\"\\nsrc_dir = \"src\"\\n\\n[dependencies]\\ntlss = {{ path = \"{repo_root / 'official' / 'nebula-tls-server'}\" }}\\n''', encoding='utf-8')\n(server_app / 'src' / 'main.nb').write_text(f'''module main\\nimport std::bytes\\nimport std::fs\\nimport std::http\\nimport std::http_json\\nimport std::json\\nimport tlss::server\\nimport tlss::http2\\n\\nasync fn main() -> Void {{\\n  match server_identity_from_pem_text(\"{lit(server_cert)}\", \"{lit(server_key)}\") {{\\n    Ok(identity) => {{\\n      let cfg0 = with_version_policy(server_config(identity), tls13_only_policy())\\n      let cfg1 = with_alpn_policy(cfg0, alpn_http2_only_policy())\\n      let cfg2 = with_client_auth_policy(cfg1, disabled_client_auth())\\n      match await bind_host(\"127.0.0.1\", {port}, cfg2) {{\\n        Ok(listener) => {{\\n          match write_string(\"{lit(str(ready_path))}\", \"ready\") {{\\n            Ok(_) => {{}}\\n            Err(msg) => {{ print(msg) }}\\n          }}\\n          match await TlsListener_accept(listener) {{\\n            Ok(stream) => {{\\n              match await TlsServerStream_read_request_h2(stream) {{\\n                Ok(request) => {{\\n                  let response = ok_json(object4(\\n                    \"path\",\\n                    string_value(request.path),\\n                    \"alpn\",\\n                    string_value(TlsServerStream_alpn_protocol(stream)),\\n                    \"tls_version\",\\n                    string_value(TlsServerStream_tls_version(stream)),\\n                    \"peer_present\",\\n                    bool_value(TlsServerStream_peer_present(stream))\\n                  ))\\n                  let write_result = await TlsServerStream_write_response_with_connection_h2(stream, response, true)\\n                  let close_result = TlsListener_close(listener)\\n                  match write_result {{\\n                    Ok(_) => {{}}\\n                    Err(msg) => {{ print(msg) }}\\n                  }}\\n                  match close_result {{\\n                    Ok(_) => {{}}\\n                    Err(msg) => {{ print(msg) }}\\n                  }}\\n                }}\\n                Err(msg) => {{\\n                  print(msg)\\n                }}\\n              }}\\n            }}\\n            Err(msg) => {{\\n              print(msg)\\n            }}\\n          }}\\n        }}\\n        Err(msg) => {{\\n          print(msg)\\n        }}\\n      }}\\n    }}\\n    Err(msg) => {{\\n      print(msg)\\n    }}\\n  }}\\n}}\\n''', encoding='utf-8')\n\n(client_app / 'nebula.toml').write_text(f'''schema_version = 1\\n\\n[package]\\nname = \"official-tls-http2-client-smoke\"\\nversion = \"0.1.0\"\\nentry = \"src/main.nb\"\\nsrc_dir = \"src\"\\n\\n[dependencies]\\ntlsc = {{ path = \"{repo_root / 'official' / 'nebula-tls'}\" }}\\n''', encoding='utf-8')\n(client_app / 'src' / 'main.nb').write_text(f'''module main\\nimport std::bytes\\nimport std::http\\nimport std::http_json\\nimport std::json\\nimport tlsc::client\\nimport tlsc::http2\\n\\nasync fn main() -> Void {{\\n  match client_config_from_ca_pem_text(\"{lit(ca_pem)}\") {{\\n    Ok(base_cfg) => {{\\n      let cfg0 = with_version_policy(base_cfg, tls13_only())\\n      let cfg1 = with_alpn_policy(cfg0, alpn_http2_only())\\n      match await dial_host(\"localhost\", {port}, cfg1) {{\\n        Ok(stream) => {{\\n          let authority = \"localhost:{port}\"\\n          let request = get_request(authority, \"/hello\")\\n          match await TlsClientStream_write_request_h2(stream, request) {{\\n            Ok(_) => {{\\n              match await TlsClientStream_read_response_h2(stream) {{\\n                Ok(response) => {{\\n                  match parse_json_body(response.body) {{\\n                    Ok(body) => {{\\n                      print(stringify(object3(\\n                        \"status\",\\n                        int_value(response.status),\\n                        \"client_alpn\",\\n                        string_value(TlsClientStream_alpn_protocol(stream)),\\n                        \"response\",\\n                        body\\n                      )))\\n                    }}\\n                    Err(msg) => {{\\n                      print(msg)\\n                    }}\\n                  }}\\n                }}\\n                Err(msg) => {{\\n                  print(msg)\\n                }}\\n              }}\\n            }}\\n            Err(msg) => {{\\n              print(msg)\\n            }}\\n          }}\\n        }}\\n        Err(msg) => {{\\n          print(msg)\\n        }}\\n      }}\\n    }}\\n    Err(msg) => {{\\n      print(msg)\\n    }}\\n  }}\\n}}\\n''', encoding='utf-8')\n\nfor app in (server_app, client_app):\n    update = subprocess.run([os.environ['NEBULA_BINARY'], 'update', str(app)], capture_output=True, text=True, env=os.environ.copy())\n    print(update.stdout, end='')\n    print(update.stderr, end='')\n    if update.returncode != 0:\n        raise SystemExit(update.returncode)\n\nserver_proc = subprocess.Popen([os.environ['NEBULA_BINARY'], 'run', str(server_app), '--run-gate', 'none'], stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True, env=os.environ.copy())\ntry:\n    deadline = time.time() + 30\n    while time.time() < deadline:\n        if ready_path.exists():\n            break\n        if server_proc.poll() is not None:\n            out, err = server_proc.communicate(timeout=5)\n            print(out, end='')\n            print(err, end='')\n            raise SystemExit(f'tls http2 server exited early rc={server_proc.returncode}')\n        time.sleep(0.1)\n    else:\n        out, err = server_proc.communicate(timeout=5)\n        print(out, end='')\n        print(err, end='')\n        raise SystemExit('tls http2 server never became ready')\n\n    client = subprocess.run([os.environ['NEBULA_BINARY'], 'run', str(client_app), '--run-gate', 'none'], capture_output=True, text=True, env=os.environ.copy())\n    print(client.stdout, end='')\n    print(client.stderr, end='')\n    if client.returncode != 0:\n        raise SystemExit(client.returncode)\n    lines = [line.strip() for line in client.stdout.splitlines() if line.strip()]\n    payload = json.loads(lines[-1])\n    if payload.get('status') != 200:\n        raise SystemExit(f'unexpected http2 client status payload: {payload!r}')\n    if payload.get('client_alpn') != 'h2':\n        raise SystemExit(f'unexpected http2 client ALPN payload: {payload!r}')\n    response = payload.get('response')\n    if not isinstance(response, dict):\n        raise SystemExit(f'missing nested response payload: {payload!r}')\n    if response.get('path') != '/hello' or response.get('alpn') != 'h2' or response.get('tls_version') != 'TLSv1.3':\n        raise SystemExit(f'unexpected http2 server payload: {payload!r}')\n    if response.get('peer_present') is not False:\n        raise SystemExit(f'unexpected http2 peer metadata payload: {payload!r}')\nfinally:\n    if server_proc.poll() is None:\n        server_proc.terminate()\n        try:\n            out, err = server_proc.communicate(timeout=5)\n        except subprocess.TimeoutExpired:\n            server_proc.kill()\n            out, err = server_proc.communicate(timeout=5)\n    else:\n        out, err = server_proc.communicate(timeout=5)\n    print(out, end='')\n    print(err, end='')\n\nprint('tls-http2-package-ok')\nPY"
expect_rc = 0
