id = "TST-180-official-tls-server-name-contract"
suite = "test"

[[steps]]
kind = "shell"
run = '''
python3 - <<'PY'
import os
import platform
import shutil
import subprocess
from pathlib import Path

system = platform.system().lower()
if system.startswith('windows') or system.startswith('msys') or system.startswith('mingw') or system.startswith('cygwin'):
    print('tls-server-name-contract-skip-unsupported-host')
    raise SystemExit(0)
if shutil.which('openssl') is None:
    raise SystemExit('openssl is required for official TLS server_name contract test')

root = Path('work/official_tls_server_name_contract')
app = root / 'app'
shutil.rmtree(root, ignore_errors=True)
(app / 'src').mkdir(parents=True, exist_ok=True)
(root / 'openssl_ca.cnf').write_text("""[req]
distinguished_name = dn
x509_extensions = v3_ca
prompt = no

[dn]
CN = Nebula TLS server_name CA

[v3_ca]
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid:always,issuer
basicConstraints = critical, CA:true
keyUsage = critical, digitalSignature, keyCertSign, cRLSign
""", encoding='utf-8')
subprocess.run([
    'openssl', 'req', '-x509', '-newkey', 'rsa:2048', '-nodes',
    '-keyout', str(root / 'ca.key'),
    '-out', str(root / 'ca.pem'),
    '-days', '2',
    '-config', str(root / 'openssl_ca.cnf'),
], check=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
(root / 'server_name.bin').write_bytes(b'localhost\x00shadow.invalid')

ca_pem = (root / 'ca.pem').read_text(encoding='utf-8')
ca_literal = ca_pem.replace('\\', '\\\\').replace('\n', '\\n').replace('"', '\\"')
server_name_path = str((root / 'server_name.bin').resolve()).replace('\\', '\\\\').replace('"', '\\"')
(app / 'nebula.toml').write_text(f"""schema_version = 1

[package]
name = "official-tls-server-name-contract"
version = "0.1.0"
entry = "src/main.nb"
src_dir = "src"

[dependencies]
tls = {{ path = "{os.environ['NEBULA_REPO_ROOT']}/official/nebula-tls" }}
""", encoding='utf-8')
(app / 'src' / 'main.nb').write_text(f"""module main
import std::bytes
import std::fs
import std::net
import tls::client

async fn main() -> Void {{
  let ok = true

  match client_config_from_ca_pem_text("{ca_literal}") {{
    Ok(config) => {{
      match await dial_host("", 1, config) {{
        Ok(_) => {{
          ok = false
          print("empty-unexpected-ok")
        }}
        Err(msg) => {{
          print(msg)
          if msg != "tls host must be non-empty" {{
            ok = false
          }}
        }}
      }}
    }}
    Err(msg) => {{
      ok = false
      print(msg)
    }}
  }}

  match read_bytes("{server_name_path}") {{
    Ok(raw_name) => {{
      match client_config_from_ca_pem_text("{ca_literal}") {{
        Ok(config) => {{
          match ipv4("127.0.0.1", 1) {{
            Ok(addr) => {{
              match await dial(addr, to_string(raw_name), config) {{
                Ok(_) => {{
                  ok = false
                  print("nul-unexpected-ok")
                }}
                Err(msg) => {{
                  print(msg)
                  if msg != "tls server_name must not contain NUL bytes" {{
                    ok = false
                  }}
                }}
              }}
            }}
            Err(msg) => {{
              ok = false
              print(msg)
            }}
          }}
        }}
        Err(msg) => {{
          ok = false
          print(msg)
        }}
      }}
    }}
    Err(msg) => {{
      ok = false
      print(msg)
    }}
  }}

  if ok {{
    print("tls-server-name-contract-ok")
  }} else {{
    print("tls-server-name-contract-bad")
  }}
}}
""", encoding='utf-8')

update = subprocess.run([os.environ['NEBULA_BINARY'], 'update', str(app)], capture_output=True, text=True, env=os.environ.copy())
print(update.stdout, end='')
print(update.stderr, end='')
if update.returncode != 0:
    raise SystemExit(update.returncode)
run = subprocess.run([os.environ['NEBULA_BINARY'], 'run', str(app), '--run-gate', 'none'], capture_output=True, text=True, env=os.environ.copy())
print(run.stdout, end='')
print(run.stderr, end='')
if run.returncode != 0:
    raise SystemExit(run.returncode)
if 'tls host must be non-empty' not in run.stdout:
    raise SystemExit('expected empty host rejection output')
if 'tls server_name must not contain NUL bytes' not in run.stdout:
    raise SystemExit('expected NUL server_name rejection output')
if 'tls-server-name-contract-ok' not in run.stdout:
    raise SystemExit('tls-server-name-contract-ok missing from output')
if 'unexpected-ok' in run.stdout or 'tls-server-name-contract-bad' in run.stdout:
    raise SystemExit('invalid server_name inputs were unexpectedly accepted')
PY
'''
expect_rc = 0
