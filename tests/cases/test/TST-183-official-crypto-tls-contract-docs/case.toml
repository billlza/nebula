id = "TST-183-official-crypto-tls-contract-docs"
suite = "test"

[[steps]]
kind = "shell"
run = "grep -F 'detailed secret-handling boundary documented in `SECURITY_CONTRACT.md`' \"$NEBULA_REPO_ROOT/official/nebula-crypto/README.md\" && grep -F '`SECURITY_CONTRACT.md` is the stronger design note' \"$NEBULA_REPO_ROOT/official/nebula-crypto/README.md\" && grep -F '`to_bytes()` is the current explicit export boundary.' \"$NEBULA_REPO_ROOT/official/nebula-crypto/SECURITY_CONTRACT.md\" && grep -F '`MlKem768Encapsulation.to_bytes()` is a secret export' \"$NEBULA_REPO_ROOT/official/nebula-crypto/SECURITY_CONTRACT.md\" && grep -F 'There is no dedicated secure-memory runtime type yet.' \"$NEBULA_REPO_ROOT/official/nebula-crypto/SECURITY_CONTRACT.md\" && grep -F 'transport evolution constraints documented in `EVOLUTION_CONTRACT.md`' \"$NEBULA_REPO_ROOT/official/nebula-tls/README.md\" && grep -F '`EVOLUTION_CONTRACT.md` records the current negative-tested boundary' \"$NEBULA_REPO_ROOT/official/nebula-tls/README.md\" && grep -F 'TLS 1.3-only servers are rejected by the current TLS 1.2 client profile' \"$NEBULA_REPO_ROOT/official/nebula-tls/EVOLUTION_CONTRACT.md\" && grep -F 'servers that require client certificates are rejected' \"$NEBULA_REPO_ROOT/official/nebula-tls/EVOLUTION_CONTRACT.md\" && grep -F 'add an explicit client-identity configuration path' \"$NEBULA_REPO_ROOT/official/nebula-tls/EVOLUTION_CONTRACT.md\" && grep -F 'secure-memory groundwork now has a named package-level contract skeleton' \"$NEBULA_REPO_ROOT/docs/post_1_0_workstreams.md\" && grep -F 'TLS groundwork now has named evolution rules and negative tests' \"$NEBULA_REPO_ROOT/docs/post_1_0_workstreams.md\""
expect_rc = 0
