id = "CHK-221-no-std-forces-strict-region"
suite = "check"
command = "check"
source = "fixtures/region_escape.nb"
args = ["--no-std", "--diag-format", "json"]
expect_rc = 1
expect_stdout_contains = ["region escape rejected in strict mode"]
forbid_stdout_contains = ["auto-promoted"]

[[expect_diag]]
code = "NBL-R001"
severity = "error"
stage = "build"
