schema_version = 1

[package]
name = "thin-host-bridge-replay"
version = "0.1.0"
entry = "src/main.nb"
src_dir = "src"

[dependencies]
bridge = { path = "../thin_host_app_core/packages/bridge" }
counter = { path = "../thin_host_app_core/packages/counter_core" }
