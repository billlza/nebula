# Thin Host Bridge Contract

Focused contract smoke for the preview thin-host app-core bridge.

It validates the current v1 command/event/snapshot envelopes, deterministic correlation and
revision fields, replay-friendly state transitions, and negative-path rejection behavior. It is not
a UI renderer or host-shell framework.
