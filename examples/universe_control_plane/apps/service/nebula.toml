schema_version = 1

[package]
name = "universe-control-service"
version = "0.1.0"
entry = "src/main.nb"
src_dir = "src"

[dependencies]
service = { installed = "nebula-service" }
observe = { installed = "nebula-observe" }
core = { path = "../../packages/core" }
