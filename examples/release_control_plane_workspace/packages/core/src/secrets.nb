module secrets
import app_config::config
import std::result

fn secret_value(env_name: String, file_env_name: String) -> Result<String, String> {
  return optional_secret_value(env_name, file_env_name)
}
