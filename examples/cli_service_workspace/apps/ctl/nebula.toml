schema_version = 1

[package]
name = "cli-service-skeleton-ctl"
version = "0.1.0"
entry = "src/main.nb"
src_dir = "src"

[dependencies]
core = { path = "../../packages/core" }
tls = { path = "../../../../official/nebula-tls" }
